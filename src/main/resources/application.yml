server:
    port: 8888
    
spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/peluconus/ConfigServer
                    default-label: master
                    search-paths:
                    - '*service'